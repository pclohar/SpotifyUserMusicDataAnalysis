const useStyles = theme => ({
    card : {
        flexDirection: 'row',
        padding: '15px',
        align: 'center'
     },

     cardImgLeft : {

     }
  });

export default withStyles(useStyles)